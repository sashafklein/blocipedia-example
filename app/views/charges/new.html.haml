= form_tag charges_path do
  = hidden_field_tag :amount, @stripe_btn_data[:amount]

  %h4 Click the button!
  %script{ class: 'stripe-button', src: "https://checkout.stripe.com/checkout.js", data: @stripe_btn_data }